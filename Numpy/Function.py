import numpy as np

#np.zeros(shape)
num=np.zeros(3)
print(num)

#(row (no. of list), shape)
a=np.zeros((2,3))
print(a)

#np.ones(shape)
no=np.ones(3)
print(no)

#np.ones((list,shape))
nm=np.ones((3,3))
print(nm)

# np.full(shape, fill_value)
b=np.full(2,5)
print(b)

#np.full((list,shape)fill_value)
c=np.full((3,4),6)
print(c)